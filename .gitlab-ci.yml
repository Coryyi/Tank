before_script:
  - docker info

build_image:
  stage: deploy
  tags:
    - task
  script:
    - docker images
    - sudo sh build.sh
  only:
    - master # this job will affect only the 'master' branch
