image: nginx:1.20.0

pages:
  stage: deploy
  tags:
    - exercise
  script:
    - mkdir .public
    - cp -r * .public
    - rm .public/.*
    - mv .public public
  artifacts:
    paths:
      - public
    expire_in: 1 week
  only:
    - master # this job will affect only the 'master' branch

