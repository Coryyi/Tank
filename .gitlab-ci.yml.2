image: docker:19.03.12

services:
  - docker:19.03.12-dind

before_script:
  - docker info

build:
  stage: build
  tags:
    - docker
  script:
    - docker build -t tank .
    - docker images
  artifacts:
    expire_in: 1 week
  only:
    - master # this job will affect only the 'master' branch
